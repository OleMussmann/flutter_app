# Key store for the signing key

Create a keystore for the signing key with the name `sign.keystore`:

```
keytool -genkey -v -keystore sign.keystore -keyalg RSA -keysize 2048 -validity 10000 -alias sign
```
