# Key store for the upload key

Create a keystore for the upload key with the name `upload.keystore`:

```
keytool -genkey -v -keystore upload.keystore -keyalg RSA -keysize 2048 -validity 10000 -alias upload
```
